#!/usr/bin/env bash

# Full CPU compaction parallelism sweep.
# Comprehensive matrix aligned with the CPU compaction study.

RUN_ID="${RUN_ID:-full-sweep}"
VALUE_SIZES="${VALUE_SIZES:-${COMPACTION_DEFAULT_VALUE_SIZES}}"
INPUT_DATA_MB_LIST="${INPUT_DATA_MB_LIST:-${COMPACTION_DEFAULT_INPUT_DATA_MB_LIST}}"
SST_SIZE_MB_LIST="${SST_SIZE_MB_LIST:-${COMPACTION_DEFAULT_SST_SIZE_MB_LIST}}"
SUBCOMP_THREADS_LIST="${SUBCOMP_THREADS_LIST:-${COMPACTION_DEFAULT_SUBCOMP_THREADS_LIST}}"
COMPACTION_RUNS="${COMPACTION_RUNS:-${COMPACTION_DEFAULT_RUNS}}"
RUN_PAUSE_SECONDS="${RUN_PAUSE_SECONDS:-2}"
