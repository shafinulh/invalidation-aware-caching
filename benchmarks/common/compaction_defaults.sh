#!/usr/bin/env bash

# Shared defaults for CPU and GPU compaction microbenchmarks.
# Sourced by both cpu_compaction and gpu_compaction sweep scripts.

# --- Sweep parameters ---
COMPACTION_DEFAULT_VALUE_SIZES="${COMPACTION_DEFAULT_VALUE_SIZES:-32 64 128 256 512 1024}"
COMPACTION_DEFAULT_RUNS="${COMPACTION_DEFAULT_RUNS:-5}"

# --- Key distribution ---
COMPACTION_DEFAULT_ZIPF_ALPHA="${COMPACTION_DEFAULT_ZIPF_ALPHA:-0.0}"
COMPACTION_DEFAULT_USER_KEY_SPACE="${COMPACTION_DEFAULT_USER_KEY_SPACE:-200000000}"

# --- GPU modes ---
COMPACTION_DEFAULT_GPU_MODES="${COMPACTION_DEFAULT_GPU_MODES:-q_paper_with_plan q_paper_without_plan c_paper_with_plan c_paper_without_plan}"

# --- CPU sweep ---
COMPACTION_DEFAULT_SST_SIZE_MB_LIST="${COMPACTION_DEFAULT_SST_SIZE_MB_LIST:-8}"
COMPACTION_DEFAULT_SUBCOMP_THREADS_LIST="${COMPACTION_DEFAULT_SUBCOMP_THREADS_LIST:-1 2 4 8 16 32}"
COMPACTION_DEFAULT_INPUT_DATA_MB_LIST="${COMPACTION_DEFAULT_INPUT_DATA_MB_LIST:-32 256}"
COMPACTION_DEFAULT_COMPACTION_BENCH="${COMPACTION_DEFAULT_COMPACTION_BENCH:-compactall}"

# --- Benchmark execution ---
COMPACTION_DEFAULT_KEY_SIZE="${COMPACTION_DEFAULT_KEY_SIZE:-16}"
COMPACTION_DEFAULT_THREADS="${COMPACTION_DEFAULT_THREADS:-1}"
COMPACTION_DEFAULT_COMPACTION_BG_THREADS="${COMPACTION_DEFAULT_COMPACTION_BG_THREADS:-1}"
COMPACTION_DEFAULT_DIRECT_IO="${COMPACTION_DEFAULT_DIRECT_IO:-true}"
COMPACTION_DEFAULT_OPEN_FILES="${COMPACTION_DEFAULT_OPEN_FILES:-512}"

# --- Metrics ---
COMPACTION_DEFAULT_COMPACTION_PERF_LEVEL="${COMPACTION_DEFAULT_COMPACTION_PERF_LEVEL:-5}"
COMPACTION_DEFAULT_METRICS_INTERVAL_MS="${COMPACTION_DEFAULT_METRICS_INTERVAL_MS:-100}"
COMPACTION_DEFAULT_HOST_METRICS_INTERVAL_SEC="${COMPACTION_DEFAULT_HOST_METRICS_INTERVAL_SEC:-0.1}"
COMPACTION_DEFAULT_REPORT_BG_IO_STATS="${COMPACTION_DEFAULT_REPORT_BG_IO_STATS:-1}"

# --- Output ---
# Graph dir is set per-sweep relative to the sweep output directory.
# No standalone default — each script places graphs inside its own output dir.
